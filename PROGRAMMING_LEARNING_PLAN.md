# Python / C / C++ / Go / Rust 12 个月学习计划

默认假设：已经会一点脚本，目标偏就业或转行，每周可投入 10-15 小时。

最快路径不是同时学 5 门语言，而是按能力层级推进：

1. Python：最快建立编程能力和项目能力。
2. C：理解内存、指针、编译、底层模型。
3. Go：练后端工程、并发、服务开发。
4. C++：在 C 的基础上理解现代大型工程。
5. Rust：在理解内存后学习所有权、生命周期和安全并发。

核心方法：每门语言只用 20% 时间看语法，80% 时间写项目。每学一门语言，都用同一套项目重写一遍，例如 CLI 工具、HTTP API、文件解析器、并发任务队列。

## 最快学习方法

- 先学共性概念，再学语言差异：变量、类型、函数、条件、循环、数组/切片、哈希表、错误处理、模块、包管理、测试、并发、I/O。
- 每天至少写 60-90 分钟代码，不要长时间只看教程。
- 每周至少提交一个可运行小程序，并写一页复盘：学了什么、踩了什么坑、和其他语言有什么不同。
- 掌握标准：不用看教程，能独立写出 CLI 工具、HTTP 服务、文件处理器、测试和 README。

每门语言用 7 步快速入门：

1. 环境安装和运行 hello world。
2. 基础语法：变量、类型、控制流、函数。
3. 核心数据结构：数组、字符串、map、struct/class。
4. 错误处理和文件/网络 I/O。
5. 包管理、测试、格式化、调试。
6. 写一个完整小项目。
7. 用这门语言重写之前做过的项目。

## 目录建议

在 `/home/chendi2721/dev` 下按语言和项目组织：

```text
dev/
  python/
    todo-cli/
    fastapi-tasks/
  c/
    dynamic-array/
    file-kv-db/
  go/
    tasks-api/
    worker-queue/
  cpp/
    kv-db-cpp/
    thread-pool/
  rust/
    file-search-cli/
    rust-http-api/
```

## 第 1-2 个月：Python 打基础

目标：能独立写脚本、API、小工具。

学习内容：

- Python 基础语法、函数、类、异常、模块。
- `venv`、`pip`、`pyproject.toml` 基础。
- 文件处理、JSON、CSV、HTTP 请求。
- `pytest` 测试。
- 基础算法和数据结构。

项目：

- 文件批量整理工具。
- 命令行 todo 工具。
- 请求公开 API 并保存为 JSON/CSV。
- FastAPI 小型 REST API：用户、任务、分页、错误处理。

验收标准：

- 能写一个带参数的 CLI。
- 能写一个带测试的 Python 包。
- 能写一个简单 HTTP API。

## 第 3-4 个月：C 理解底层

目标：理解程序如何接近机器运行。

学习内容：

- 编译流程：预处理、编译、链接。
- 指针、数组、字符串、struct。
- 栈、堆、`malloc/free`。
- 文件 I/O。
- Makefile。
- 基础调试：`gdb`、地址错误、内存泄漏。
- 常见错误：越界、悬垂指针、重复释放。

项目：

- 手写动态数组。
- 手写字符串工具库。
- 简单 key-value 文件数据库。
- 简单 shell 命令解析器。

验收标准：

- 能解释指针和数组的区别。
- 能用 C 写一个可编译的多文件项目。
- 能用调试器定位崩溃。
- 能说清楚内存分配和释放责任。

## 第 5-6 个月：Go 做后端工程

目标：能用 Go 写可部署的后端服务。

学习内容：

- Go 基础语法、struct、interface。
- `go mod` 包管理。
- 错误处理。
- goroutine、channel、context。
- HTTP 服务、JSON、数据库访问。
- 单元测试和集成测试。
- 日志、配置、Docker 部署。

项目：

- REST API 服务：用户、任务、认证、数据库。
- 并发下载器。
- 日志分析工具。
- 小型后台 worker：队列、重试、超时控制。

验收标准：

- 能写一个结构清晰的 Go Web API。
- 能理解 goroutine 泄漏和 context 取消。
- 能给核心逻辑写测试。
- 能用 Docker 跑起来。

## 第 7-9 个月：C++ 进入大型工程

目标：理解现代 C++，避免只停留在 C with classes。

学习内容：

- C++ 类型、引用、RAII。
- class、构造/析构、拷贝/移动语义。
- STL：`vector`、`string`、`unordered_map`、算法库。
- 智能指针：`unique_ptr`、`shared_ptr`。
- 模板基础。
- CMake。
- 单元测试。
- 基础多线程。

项目：

- 用 C++ 重写 C 的 key-value 数据库。
- 简单日志库。
- 线程池。
- 小型 HTTP 客户端或命令行工具。

验收标准：

- 能解释 RAII、拷贝、移动、智能指针。
- 能用 CMake 管理项目。
- 能写现代 C++，少用裸 `new/delete`。
- 能看懂中等复杂度 C++ 项目结构。

## 第 10-12 个月：Rust 学安全系统编程

目标：理解所有权模型，并能写可靠 CLI/服务。

学习内容：

- 所有权、借用、生命周期。
- `Result`、`Option`、错误传播。
- struct、enum、trait。
- Cargo、crate、feature。
- iterator、pattern matching。
- async Rust 基础。
- `tokio`、`serde`、`reqwest`、`axum` 或 `actix-web`。
- 测试和 benchmark 基础。

项目：

- Rust CLI 文件搜索工具。
- JSON/CSV 解析器。
- 并发下载器。
- 小型 Rust HTTP API。
- 用 Rust 重写 Go 的一个后端服务核心模块。

验收标准：

- 能读懂 borrow checker 报错并修复。
- 能写带测试的 Cargo 项目。
- 能用 Rust 写 CLI 或 HTTP 服务。
- 能解释 Rust 相比 C/C++ 解决了什么问题。

## 每周节奏

每周 10-15 小时建议这样分配：

- 2 小时：看书、文档或教程，只学当前项目需要的内容。
- 6-8 小时：写代码，完成项目功能。
- 1-2 小时：调试、测试、重构。
- 1 小时：复盘和笔记。
- 1-2 小时：刷基础题或阅读优秀代码。

每天最小学习单元：

- 15 分钟：复习昨天代码。
- 45-90 分钟：写新功能。
- 10 分钟：记录一个语言差异点。

## 每月验收方式

每个月结束时做一次验收：

- 不看教程，从零创建项目。
- 写 README，说明怎么安装、运行、测试。
- 至少有 5 个测试用例。
- 至少处理 3 类错误输入。
- 能向别人解释这个项目的设计。
- 把同一个项目用两门语言实现，对比语法、错误处理、性能、工程体验。

最终 12 个月作品集：

- Python：CLI 工具 + FastAPI 服务。
- C：数据结构库 + 文件型 key-value 存储。
- Go：可部署 REST API + 并发 worker。
- C++：线程池或日志库 + CMake 项目。
- Rust：CLI 工具 + 小型 HTTP API。

## 时间调整

- 每周只有 5-8 小时：把计划拉长到 18-24 个月。
- 每周能投入 20 小时以上：可以压缩到 6-9 个月，但项目和复盘不能省。
